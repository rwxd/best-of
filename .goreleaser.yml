---
version: 2
project_name: best-of

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - env:
      - CGO_ENABLED=0
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
    goarch:
      - amd64
      - arm64
    goos:
      - linux
      - darwin
checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_SHA256SUMS"
  algorithm: sha256
release:
  draft: false
changelog:
  disable: true
nfpms:
  - package_name: "best-of"
    maintainer: rwxd <git@rwxd.de>
    description: "Check the runtime of program executions"
    formats:
      - deb
      - rpm
